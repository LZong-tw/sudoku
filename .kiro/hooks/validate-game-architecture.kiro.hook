{
  "enabled": true,
  "name": "Validate Game Architecture",
  "description": "Validates Sudoku architecture patterns, modular design, and EventBus usage when source files are edited",
  "version": "1",
  "when": {
    "type": "fileEdited",
    "patterns": [
      "src/*.js",
      "src/**/*.js"
    ]
  },
  "then": {
    "type": "askAgent",
    "prompt": "Review the edited file for:\n1. Architecture patterns - ensure proper separation (GameController, GridModel, UI views, Features, Utils)\n2. EventBus usage - verify events are emitted/listened correctly, no direct component coupling\n3. Modular design - verify single responsibility and clear interfaces\n4. Code quality - check for proper encapsulation, minimal coupling, adherence to existing patterns\n\nProvide specific feedback if any issues are found."
  }
}
